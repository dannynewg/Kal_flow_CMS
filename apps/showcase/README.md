# GitHub Pages showcase

This directory contains the static, mock-data development showcase published to GitHub Pages. It intentionally has no credentials, server API, database connection, or real authentication.

The executable Kal_flow application remains in `apps/web`, `apps/api`, and `apps/worker`. Persistent staging will deploy those services after the first dashboard and contract module are production-ready.

generator client {
  provider = "prisma-client"
  output   = "../generated/client"
}

datasource db {
  provider = "postgresql"
}

// Domain models and their first migration are intentionally added with the
// organization-management phase so tenant invariants are designed together.
